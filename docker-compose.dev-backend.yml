version: '3.8'

services:
  backend:
    environment:
      REDIS_URL: ""   # Disable Redis for development
      DEBUG: "true"
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --reload-dir /app
    depends_on: []  # Remove Redis dependency
