# Backend environment variables
DEBUG=false
METADATA_ROOT=/app/metadata

# Required: base URL of the cerevi-dc-helper HTTP file server.
# When running everything via docker-compose alongside dc-helper, use
# the service name, e.g. http://dc-helper:9000
DC_HELPER_URL=http://dc-helper:9000
